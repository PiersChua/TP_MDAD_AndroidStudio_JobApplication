<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="16dp"
            android:paddingHorizontal="16dp"
            android:paddingBottom="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvJobApplicationCardTitle"
                    style="@style/Text.Medium"
                    android:ellipsize="end"
                    android:singleLine="true"
                    android:text="Position"
                    android:textStyle="bold" />
                <TextView
                    android:id="@+id/tvJobApplicationCardAgencyName"
                    style="@style/Text.Small"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:textColor="@color/placeholder_foreground"
                    android:text="Agency Name"
                    android:textStyle="bold" />

            </LinearLayout>


            <TextView
                android:id="@+id/tvJobApplicationCardSalary"
                style="@style/Text.Small"
                android:layout_marginTop="8dp"
                android:drawablePadding="2dp"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:singleLine="true"
                android:text="Salary"
                android:textColor="@color/secondary"
                android:textStyle="bold"
                app:drawableStartCompat="@drawable/ic_money_18" />

            <TextView
                android:id="@+id/tvJobApplicationCardLocations"
                style="@style/Text.Small"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:singleLine="true"
                android:text="Locations"
                app:drawableStartCompat="@drawable/ic_location_18" />

        </LinearLayout>

        <com.google.android.material.divider.MaterialDivider
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            app:dividerColor="@color/placeholder_background" />
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="16dp">

            <TextView
                android:id="@+id/tvJobApplicationCardStatus"
                style="@style/Text.Small"
                android:drawablePadding="2dp"
                android:ellipsize="end"
                android:singleLine="true"
                android:textStyle="bold"
                android:text="PENDING"
                app:drawableStartCompat="@drawable/ic_info_18" />
            <TextView
                android:id="@+id/tvJobApplicationCardCreatedAt"
                style="@style/Text.ExtraSmall"
                android:drawablePadding="2dp"
                android:ellipsize="end"
                android:singleLine="true"
                android:text="Submitted on 19/2/2024"
                app:drawableStartCompat="@drawable/ic_time_16" />
        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>